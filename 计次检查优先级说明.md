# 计次检查优先级说明

## 📋 检查顺序的重要性

根据用户要求，计次检查必须按照**优先级顺序**执行，这样可以排除错误的判断情况。

---

## 🎯 优先级定义

### 🔴 **优先级高**：计次最多只能+1（原则2）
- **检查内容**：本轮和上一轮，计次增量是否 > 1
- **修正方式**：如果超过1，修正为上一轮+1
- **执行顺序**：**必须先执行**

### 🟡 **优先级中**：计次当天不能减小（原则1）
- **检查内容**：计次是否比上一轮减小
- **修正方式**：如果减小，保持上一轮的值
- **执行顺序**：**在优先级高之后执行**

---

## 💡 为什么要按优先级顺序？

### 关键原因：排除错误判断

如果不按优先级顺序，可能会出现错误判断：

#### ❌ 错误顺序示例（先检查减小，再检查增加>1）
```
场景：上一轮计次=5，本轮计次=10

步骤1：检查是否减小
  → 10 - 5 = +5，未减小 ✓

步骤2：检查是否增加>1
  → 增加5 > 1，修正为6 ✓

结果：最终值=6 ✓（看似正确）
```

但如果遇到特殊情况：
```
场景：上一轮计次=10，本轮计次=3

步骤1：检查是否减小
  → 3 < 10，减小了！修正为10 ✓

步骤2：检查是否增加>1
  → 10 - 10 = 0，未增加 ✓

结果：最终值=10 ✓（正确）
```

#### ✅ 正确顺序示例（先检查增加>1，再检查减小）
```
场景：上一轮计次=5，本轮计次=10

步骤1：🔴 检查是否增加>1
  → 10 - 5 = +5 > 1，修正为6 ✓

步骤2：🟡 检查是否减小
  → 6 - 5 = +1，未减小 ✓

结果：最终值=6 ✓（正确）
```

```
场景：上一轮计次=5，本轮计次=3

步骤1：🔴 检查是否增加>1
  → 3 - 5 = -2，未超过1 ✓

步骤2：🟡 检查是否减小
  → 3 < 5，减小了！修正为5 ✓

结果：最终值=5 ✓（正确）
```

---

## 🔄 执行流程图

```
开始
  ↓
获取本轮计次 new_count
  ↓
计算增量 = new_count - last_count
  ↓
┌─────────────────────────────┐
│ 🔴 优先级高：检查增加>1     │
│ if 增量 > 1:                │
│   修正为 last_count + 1     │
│   更新 new_count             │
│   重新计算增量               │
└─────────────────────────────┘
  ↓
┌─────────────────────────────┐
│ 🟡 优先级中：检查减小       │
│ if new_count < last_count:  │
│   修正为 last_count          │
└─────────────────────────────┘
  ↓
存入数据库
  ↓
结束
```

---

## 📊 测试场景

### 场景1：正常增加
- **输入**：上一轮=5，本轮=6
- **步骤1**：增加1，不超过1 ✓
- **步骤2**：未减小 ✓
- **结果**：最终=6 ✅

### 场景2：保持不变
- **输入**：上一轮=5，本轮=5
- **步骤1**：增加0，不超过1 ✓
- **步骤2**：未减小 ✓
- **结果**：最终=5 ✅

### 场景3：增加过多（原则2违规）
- **输入**：上一轮=5，本轮=10
- **步骤1**：增加5 > 1 → 修正为6 🔧
- **步骤2**：未减小 ✓
- **结果**：最终=6 ✅

### 场景4：减小（原则1违规）
- **输入**：上一轮=5，本轮=3
- **步骤1**：增加-2，不超过1 ✓
- **步骤2**：减少2 → 修正为5 🔧
- **结果**：最终=5 ✅

### 场景5：增加2（原则2违规）
- **输入**：上一轮=5，本轮=7
- **步骤1**：增加2 > 1 → 修正为6 🔧
- **步骤2**：未减小 ✓
- **结果**：最终=6 ✅

---

## 📝 代码实现（collect_and_store.py）

### 优先级检查代码（第446-498行）

```python
# ===== 计次检查（按优先级顺序执行） =====
# 优先级高：原则2 - 计次最多增加1（先执行）
# 优先级中：原则1 - 计次不能减小（后执行）
new_count = int(data.get('计次', 0))

if not is_new_day_reset and last_count > 0:
    count_change = new_count - last_count
    
    # 🔴 优先级高：原则2 - 计次最多增加1（先检查和修正）
    if count_change > 1:
        print(f"\n⚠️  【优先级高】计次增加过多检测！")
        print(f"   上一条记录 ({last_time}): 计次={last_count}")
        print(f"   当前数据（源文件）: 计次={new_count}")
        print(f"   ❌ 计次增加了 {count_change}（超过最大允许值1）")
        
        # 自动修正：最多只能增加1
        corrected_count = last_count + 1
        print(f"   🔧 自动修正：计次 {new_count} → {corrected_count}（最多增加1）")
        data['计次'] = str(corrected_count)
        new_count = corrected_count  # 更新new_count用于后续检查
        
        # 重新计算计次得分
        # ...
        
        # 重新计算count_change用于下一步检查
        count_change = new_count - last_count
    
    # 🟡 优先级中：原则1 - 计次不能减小（在优先级高之后检查）
    if count_change < 0:
        print(f"\n⚠️  【优先级中】计次减小检测！")
        print(f"   上一条记录 ({last_time}): 计次={last_count}")
        print(f"   当前数据（修正后）: 计次={new_count}")
        print(f"   ❌ 计次减少了 {abs(count_change)}（违反原则1：计次不能减小）")
        
        # 自动修正：保持不变
        print(f"   🔧 自动修正：计次 {new_count} → {last_count}（保持不变）")
        data['计次'] = str(last_count)
        
        # 重新计算计次得分
        # ...
```

---

## ✅ 优先级顺序的优势

### 1. 逻辑清晰
- 🔴 先处理上限问题（最多+1）
- 🟡 再处理下限问题（不能减小）

### 2. 避免错误判断
- 确保修正后的值符合所有规则
- 不会出现互相冲突的修正

### 3. 易于维护
- 代码结构清晰
- 优先级明确标注
- 便于理解和扩展

### 4. 保证数据质量
- 无论源数据如何错误
- 最终存储的数据100%合规

---

## 🎯 总结

### 正确的执行顺序
1. 🔴 **优先级高**：检查计次增加是否>1 → 修正为+1
2. 🟡 **优先级中**：检查计次是否减小 → 保持不变

### 为什么这样做？
✅ **先修正增加过多，再检查是否减小**  
✅ **排除错误的判断情况**  
✅ **确保最终结果符合两个原则**

---

**文档生成时间**：2025-12-08  
**实施状态**：✅ 已完成并测试通过  
**相关提交**：commit `e0c6863`
