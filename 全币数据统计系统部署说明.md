# 全币数据统计系统 - 部署说明

## 🎯 系统概述

全币数据统计系统是一个自动化的加密货币数据采集、存储和可视化系统。系统每分钟自动采集全币数据（包括急涨、急跌、涨跌情况等），并提供历史数据回看功能，支持按日期查询和时间轴回放。

---

## 📊 系统架构

```
全币数据统计系统
├── 数据采集服务 (coin_history_collector.py)
│   └── 每60秒采集一次数据
├── 数据存储 (coin_history_stats.db)
│   ├── coin_snapshots - 快照数据
│   ├── coin_details - 币种详情
│   └── daily_summary - 日期汇总
├── API服务 (coin_history_api.py)
│   └── RESTful API接口 (Port 5002)
└── 前端界面 (coin_history_viewer.html)
    └── 可视化历史数据回看
```

---

## 🚀 访问地址

### 📱 前端页面
**历史数据回看界面:**
```
https://8080-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai/coin_history_viewer.html
```

### 🔌 API接口
**API Base URL:**
```
https://5002-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai
```

**主要接口:**
- `GET /api/health` - 健康检查
- `GET /api/history/dates` - 获取有数据的日期列表
- `GET /api/history/snapshots?date=YYYY-MM-DD` - 获取指定日期的所有快照
- `GET /api/history/snapshot/<datetime>` - 获取指定快照的详细数据
- `GET /api/history/timeline?date=YYYY-MM-DD` - 获取时间轴数据（用于图表）
- `GET /api/history/search?symbol=BTC&date=YYYY-MM-DD` - 搜索币种历史
- `GET /api/history/stats` - 获取统计数据

---

## ⚙️ 系统状态

### 运行中的服务

| 服务名称 | 状态 | 端口 | 说明 |
|---------|------|------|------|
| 数据采集服务 | ✅ 运行中 | - | 每60秒采集一次 |
| API服务 | ✅ 运行中 | 5002 | RESTful API |
| 静态文件服务 | ✅ 运行中 | 8080 | 前端页面服务 |

### 数据统计

```bash
# 当前采集状态 (实时更新)
Total Days: 1
Total Snapshots: 8+
Average Coins per Snapshot: 29
```

---

## 📊 功能特性

### 1. 数据采集
- **自动采集**: 每分钟自动从主系统采集数据
- **数据来源**: `http://localhost:5000/api/latest`
- **采集内容**:
  - 全币总数
  - 急涨数量 (rush_up_count)
  - 急跌数量 (rush_down_count)
  - 上涨币种数
  - 下跌币种数
  - 每个币种的详细数据

### 2. 数据存储
- **数据库**: SQLite (coin_history_stats.db)
- **三张主表**:
  1. **coin_snapshots**: 每分钟的快照数据
  2. **coin_details**: 每个币种的详细信息
  3. **daily_summary**: 按日期汇总的统计信息

### 3. 历史回看
- **按日期查询**: 选择日期查看当天所有历史数据
- **时间轴展示**: 以时间轴形式展示数据变化趋势
- **详细快照**: 点击任意时间点查看完整的快照数据
- **图表可视化**: 使用Chart.js展示数据趋势
- **币种搜索**: 搜索特定币种的历史数据

### 4. API接口
- **RESTful设计**: 标准的REST API
- **CORS支持**: 支持跨域请求
- **JSON格式**: 所有数据采用JSON格式
- **完整文档**: 提供详细的API文档

---

## 🔧 技术栈

### 后端
- **Python 3.12**
- **Flask** - Web框架
- **Flask-CORS** - 跨域支持
- **SQLite3** - 数据库
- **Requests** - HTTP客户端

### 前端
- **原生JavaScript**
- **Chart.js** - 图表库
- **CSS3** - 样式设计
- **Responsive Design** - 响应式布局

---

## 📝 数据库结构

### coin_snapshots (快照表)
```sql
id INTEGER PRIMARY KEY
snapshot_date TEXT          -- 快照日期 (YYYY-MM-DD)
snapshot_time TEXT          -- 快照时间 (HH:MM:SS)
snapshot_datetime TEXT      -- 完整日期时间
total_coins INTEGER         -- 币种总数
rush_up_count INTEGER       -- 急涨数量
rush_down_count INTEGER     -- 急跌数量
rising_coins INTEGER        -- 上涨币种数
falling_coins INTEGER       -- 下跌币种数
account_ratio_avg REAL      -- 平均账户比例
account_ratio_max REAL      -- 最大账户比例
account_ratio_min REAL      -- 最小账户比例
avg_change_24h REAL         -- 24h平均涨跌
max_change_24h REAL         -- 24h最大涨幅
min_change_24h REAL         -- 24h最大跌幅
data_source TEXT            -- 数据来源
created_at TIMESTAMP        -- 创建时间
```

### coin_details (币种详情表)
```sql
id INTEGER PRIMARY KEY
snapshot_datetime TEXT      -- 关联的快照时间
symbol TEXT                 -- 币种符号
rank INTEGER                -- 排名
price REAL                  -- 价格
change_24h REAL             -- 24h涨跌幅
rush_up INTEGER             -- 急涨次数
rush_down INTEGER           -- 急跌次数
account_ratio REAL          -- 账户比例
volume_24h REAL             -- 24h交易量
market_cap REAL             -- 市值
```

### daily_summary (日期汇总表)
```sql
id INTEGER PRIMARY KEY
summary_date TEXT           -- 汇总日期
total_snapshots INTEGER     -- 当天快照总数
avg_total_coins REAL        -- 平均币种数
max_rush_up INTEGER         -- 最大急涨数
max_rush_down INTEGER       -- 最大急跌数
highest_change REAL         -- 最高涨幅
lowest_change REAL          -- 最低跌幅
first_snapshot_time TEXT    -- 首个快照时间
last_snapshot_time TEXT     -- 最后快照时间
updated_at TIMESTAMP        -- 更新时间
```

---

## 🛠️ 运维命令

### 查看服务状态
```bash
# 查看采集服务
ps aux | grep coin_history_collector

# 查看API服务
ps aux | grep coin_history_api

# 查看HTTP服务
ps aux | grep "http.server.*8080"
```

### 查看日志
```bash
# 采集服务日志
tail -f /home/user/webapp/coin_history_collector.log

# API服务日志
tail -f /home/user/webapp/coin_history_api.log
```

### 查看数据库
```bash
# 进入项目目录
cd /home/user/webapp

# 查看数据统计
python3 << 'EOF'
import sqlite3
conn = sqlite3.connect('coin_history_stats.db')
cursor = conn.cursor()

cursor.execute("SELECT COUNT(*) FROM coin_snapshots")
print(f"快照总数: {cursor.fetchone()[0]}")

cursor.execute("SELECT COUNT(DISTINCT snapshot_date) FROM coin_snapshots")
print(f"数据天数: {cursor.fetchone()[0]}")

cursor.execute("SELECT snapshot_date, COUNT(*) FROM coin_snapshots GROUP BY snapshot_date")
for row in cursor.fetchall():
    print(f"  {row[0]}: {row[1]} 次快照")
    
conn.close()
EOF
```

### 重启服务
```bash
# 停止所有服务
pkill -f coin_history_collector
pkill -f coin_history_api

# 重新启动
cd /home/user/webapp
nohup python3 -u coin_history_collector.py > coin_history_collector.log 2>&1 &
nohup python3 -u coin_history_api.py > coin_history_api.log 2>&1 &
```

---

## 📈 使用示例

### 1. 查看有数据的日期
```bash
curl https://5002-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai/api/history/dates
```

### 2. 获取指定日期的快照
```bash
curl "https://5002-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai/api/history/snapshots?date=2025-12-08"
```

### 3. 获取时间轴数据
```bash
curl "https://5002-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai/api/history/timeline?date=2025-12-08"
```

### 4. 搜索币种历史
```bash
curl "https://5002-iik759kgm7i3zqlxvfrfx-cc2fbc16.sandbox.novita.ai/api/history/search?symbol=BTC&date=2025-12-08"
```

---

## ⚠️ 注意事项

1. **数据采集间隔**: 系统每60秒采集一次，确保数据连续性
2. **数据库备份**: 定期备份 `coin_history_stats.db` 文件
3. **磁盘空间**: 注意监控磁盘空间，数据会持续增长
4. **端口占用**: 确保5002和8080端口不被其他服务占用
5. **依赖服务**: 系统依赖主系统的 `http://localhost:5000/api/latest` 接口

---

## 🎯 下一步计划

- [ ] 添加数据导出功能 (CSV/Excel)
- [ ] 添加数据清理策略 (自动删除N天前的数据)
- [ ] 添加更多统计图表
- [ ] 添加实时数据推送 (WebSocket)
- [ ] 添加告警功能 (异常数据通知)
- [ ] 性能优化 (数据库索引、缓存)

---

## 📞 支持

如有问题或建议，请查看：
- 项目文档: `COIN_HISTORY_SYSTEM_README.md`
- Git仓库: [genspark_ai_developer 分支]

---

**最后更新**: 2025-12-08
**系统状态**: ✅ 运行正常
**数据采集**: ✅ 持续采集中
