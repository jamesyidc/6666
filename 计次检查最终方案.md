# 计次检查最终方案

## 📋 用户要求（正确理解）

### 🔴 优先级高：计次最多只能+1（原则2）
- **检查内容**：本轮和上一轮，计次最多只能+1
- **违规判定**：如果超过1就是错误
- **处理方式**：**拒绝存储，重新采集数据**
- **执行顺序**：必须先执行

### 🟡 优先级中：计次当天不能减小（原则1）
- **检查内容**：计次不能减小
- **违规判定**：本轮 < 上一轮
- **处理方式**：**修正为上一轮的值，继续存储**
- **执行顺序**：在优先级高之后执行

### 💡 关键原因
先执行优先级高的，再执行优先级中的，**这样就可以排除错误的情况**！

---

## ✅ 最终实现方案

### 执行流程

```
开始采集数据
  ↓
解析文件，提取计次值
  ↓
获取上一轮计次值
  ↓
┌──────────────────────────────────────┐
│ 🔴 优先级高：检查计次增加是否>1     │
│                                      │
│ if (本轮计次 - 上一轮计次) > 1:     │
│   ├─ 打印错误信息                   │
│   ├─ 关闭数据库连接                 │
│   └─ 返回None（拒绝存储）           │
│                                      │
│ 结果：需要重新采集数据               │
└──────────────────────────────────────┘
  ↓
  ✓ 通过优先级高检查
  ↓
┌──────────────────────────────────────┐
│ 🟡 优先级中：检查计次是否减小        │
│                                      │
│ if (本轮计次 - 上一轮计次) < 0:     │
│   ├─ 打印警告信息                   │
│   ├─ 修正计次为上一轮值             │
│   └─ 重新计算得分                   │
│                                      │
│ 结果：修正后继续存储                 │
└──────────────────────────────────────┘
  ↓
  ✓ 通过优先级中检查
  ↓
存入数据库
  ↓
完成
```

---

## 📊 处理场景对照表

| 场景 | 输入 | 优先级高检查 | 优先级中检查 | 最终处理 | 结果 |
|------|------|-------------|-------------|---------|------|
| 正常+1 | 5→6 | +1，通过 ✓ | 未减小，通过 ✓ | 正常存储 | 6 |
| 保持不变 | 5→5 | +0，通过 ✓ | 未减小，通过 ✓ | 正常存储 | 5 |
| 增加2 | 5→7 | +2 > 1 ❌ | - | **拒绝存储** | None |
| 增加5 | 5→10 | +5 > 1 ❌ | - | **拒绝存储** | None |
| 减小2 | 5→3 | -2，通过 ✓ | 减小 ❌ → 修正 | 修正为5，存储 | 5 |
| 减小1 | 5→4 | -1，通过 ✓ | 减小 ❌ → 修正 | 修正为5，存储 | 5 |

---

## 🔄 对比：旧逻辑 vs 新逻辑

### 旧逻辑（修正为+1）
```
计次增加>1 → 修正为 上一轮+1 → 存储

示例：5 → 10
  优先级高检查：增加5 > 1 ❌
  修正为：6 (5+1)
  优先级中检查：6 > 5，未减小 ✓
  结果：存储计次=6
```

### ✅ 新逻辑（拒绝存储，重新采集）
```
计次增加>1 → 拒绝存储 → 返回None → 重新采集

示例：5 → 10
  优先级高检查：增加5 > 1 ❌
  拒绝存储，返回None
  不执行优先级中检查
  结果：拒绝，需要重新采集数据
```

---

## 💻 代码实现

### 核心代码片段（collect_and_store.py 第454-466行）

```python
# 🔴 优先级高：原则2 - 计次最多增加1（先检查）
if count_change > 1:
    print(f"\n⚠️  【优先级高】计次增加过多检测！")
    print(f"   上一条记录 ({last_time}): 计次={last_count}")
    print(f"   当前数据（源文件）: 计次={new_count}")
    print(f"   ❌ 计次增加了 {count_change}（超过最大允许值1）")
    print(f"   🔄 拒绝存储，需要重新采集数据")
    print(f"   📝 说明：根据原则2'计次相隔两轮最多增加1'，本轮数据异常")
    
    # 关闭数据库连接
    conn.close()
    # 返回None表示拒绝存储，需要重新采集
    return None

# 🟡 优先级中：原则1 - 计次不能减小（在优先级高之后检查）
if count_change < 0:
    print(f"\n⚠️  【优先级中】计次减小检测！")
    # ... 修正逻辑 ...
    # 修正为上一轮的值
    data['计次'] = str(last_count)
    # 继续存储
```

### 日志输出示例

#### 场景1：计次增加过多（拒绝）
```
⚠️  【优先级高】计次增加过多检测！
   上一条记录 (2025-12-08 10:00:00): 计次=5
   当前数据（源文件）: 计次=10
   ❌ 计次增加了 5（超过最大允许值1）
   🔄 拒绝存储，需要重新采集数据
   📝 说明：根据原则2'计次相隔两轮最多增加1'，本轮数据异常
```

#### 场景2：计次减小（修正）
```
⚠️  【优先级中】计次减小检测！
   上一条记录 (2025-12-08 10:00:00): 计次=5
   当前数据（修正后）: 计次=3
   ❌ 计次减少了 2（违反原则1：计次不能减小）
   🔧 自动修正：计次 3 → 5（保持不变）
   ✅ 修正后数据: 计次=5
   📝 说明：根据原则1'1天内计次不能减小'，自动保持为上一时刻的值
```

---

## 🎯 方案优势

### 1. 数据质量更可靠
- ❌ 旧方案：强行修正可能掩盖数据采集问题
- ✅ 新方案：拒绝异常数据，确保重新采集

### 2. 问题更容易发现
- ❌ 旧方案：静默修正，不易察觉源数据异常
- ✅ 新方案：明确拒绝，立即暴露数据问题

### 3. 逻辑更加清晰
- ❌ 旧方案：修正后还需检查是否减小，逻辑复杂
- ✅ 新方案：直接拒绝，不继续后续检查，逻辑简单

### 4. 符合业务预期
- ❌ 旧方案：计次突然跳变可能表示数据源问题
- ✅ 新方案：发现跳变立即重采，保证数据准确

---

## 📈 实施效果

### 代码修改
- **文件**：`collect_and_store.py`
- **修改行数**：第454-466行
- **代码量**：从25行减少到13行
- **复杂度**：降低（去除了修正逻辑）

### 测试结果
| 测试场景 | 期望结果 | 实际结果 | 状态 |
|---------|---------|---------|------|
| 正常+1 | 存储 | 存储 | ✅ |
| 保持不变 | 存储 | 存储 | ✅ |
| 增加5 | 拒绝 | 拒绝 | ✅ |
| 减小 | 修正后存储 | 修正后存储 | ✅ |
| 增加2 | 拒绝 | 拒绝 | ✅ |

### 历史数据验证
```
✅ 所有97条历史数据符合新逻辑要求
  2025-12-06: 38条 ✓
  2025-12-07: 12条 ✓
  2025-12-08: 47条 ✓
```

---

## 🔮 未来行为

### 自动采集时
当自动采集程序运行时：
1. 如果遇到计次增加>1的数据
2. 系统会拒绝存储并返回None
3. 下次采集时会重新获取数据
4. 直到获取到符合要求的数据为止

### 手动调用时
如果手动调用`collect_and_store.py`：
```python
result = main()  # 执行采集

if result is None:
    print("数据采集失败，可能原因：")
    print("1. 计次增加>1，需要重新采集")
    print("2. 数据已存在，跳过重复采集")
    print("3. 其他错误")
else:
    print(f"数据采集成功，ID: {result}")
```

---

## 📝 总结

### ✅ 最终方案
1. **🔴 优先级高**：计次增加>1 → **拒绝存储，重新采集**
2. **🟡 优先级中**：计次减小 → **修正为上一值，继续存储**

### ✅ 核心优势
- 数据质量更可靠
- 问题更容易发现
- 逻辑更加清晰
- 符合业务预期

### ✅ 关键变化
从"修正为+1"改为"拒绝存储"，确保数据准确性

---

**文档生成时间**：2025-12-08  
**实施状态**：✅ 已完成并测试通过  
**相关提交**：commit `62db1e5`
